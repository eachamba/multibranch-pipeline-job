pipeline {
   
   agent any;

    stages {
        stage('testing job') {
            steps {
                sh 'echo "hello from erankline-acha !!"'
                sh 'date'
                sh 'cal'
            }
        }
    }
}
