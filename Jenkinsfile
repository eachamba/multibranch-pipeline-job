pipeline {
   
   agent any;

    stages {
        stage('testing from new work job') {
            steps {
                sh 'echo "hello from  the new erankline-acha branch !!"'
                sh 'date'
                sh 'cal'
            }
        }
    }
}
